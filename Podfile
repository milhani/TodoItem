# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
workspace 'TodoItemApp'
target 'TodoItem (YSHMR)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TodoItem (YSHMR)
pod 'SwiftLint'
  target 'TodoItem (YSHMR)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TodoItem (YSHMR)UITests' do
    # Pods for testing
  end

end
